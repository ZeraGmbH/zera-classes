#ifndef GAUSSNODE_H
#define GAUSSNODE_H

/**
  @b class for a data point
  */
struct cGaussNode {
    /**
      @b the data point's value
      */
    double m_fNode;
    /**
      @b the data point's argument
      */
    double m_fArg;
};

#endif // GAUSSNODE_H
